# Response headers for hosts that read a _headers file (Cloudflare Pages,
# Netlify). GitHub Pages cannot set headers and ignores this file.
#
# Real cross-origin isolation from the host, so SharedArrayBuffer works
# without the coi-serviceworker shim. index.html still loads that shim,
# but it registers nothing once the page is already cross-origin isolated
# (it checks window.crossOriginIsolated first), so it is a no-op here and
# the fallback on GitHub Pages. Measured, that is worth about half a
# second on a first-ever visit -- the reload the shim performs -- and
# nothing afterwards; docs/engine.md carries the numbers.
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp

# The module tree is renamed js.<hash>/ per deploy, and the engine, guest
# and vendor files are fetched with a content-hash query string, so the
# bytes behind any one URL never change. Cache them forever. The fonts
# are subsetted files that are replaced rather than edited, so they get
# the same treatment.
/js.*
  Cache-Control: public, max-age=31536000, immutable
/vendor/*
  Cache-Control: public, max-age=31536000, immutable
/qemu/*
  Cache-Control: public, max-age=31536000, immutable
/guest/*
  Cache-Control: public, max-age=31536000, immutable
/fonts/*
  Cache-Control: public, max-age=31536000, immutable

# The entry point and the manifest name those hashes, so they must be
# revalidated on every load or a new deploy would never be seen. The
# index is rebuilt every few hours at the same URLs, and a stale shard
# names package files the mirrors have already deleted, so it is
# revalidated too. about.html is plain prose, but it is edited in place
# like any page and shares the stylesheet, so it is revalidated with it.
/index.html
  Cache-Control: no-cache
/about.html
  Cache-Control: no-cache
/assets.json
  Cache-Control: no-cache
/index/*
  Cache-Control: no-cache
